# دليل النشر — من مجلّدك إلى الإنترنت

اتّبع الخطوات بالترتيب. كل أمر يُنسخ ويُلصق كما هو.
لا تحتاج إلى خبرة برمجية — تحتاج فقط أن تفتح نافذة أوامر.

---

## أين المجلّد

المشروع كلّه في مجلّد اسمه **`falak-web`** داخل مجلّد مخرجات Cowork على جهازك:

```
C:\Users\utilisateur\AppData\Roaming\Claude\local-agent-mode-sessions\96e75ceb-22d0-4aca-9b08-28c8b9713f49\e64806a4-0aa9-494f-99e7-fe49aa7366e9\local_dd5eae76-6023-4266-b52e-70454026525f\outputs\falak-web
```

**أسهل طريقة للوصول إليه:** اضغط على أي ملف عرضته لك في المحادثة، ثم اختر
«إظهار في المجلّد» (Show in folder). سيفتح مستكشف الملفات على مكانه.

**اقتراح:** انسخ مجلّد `falak-web` إلى مكان أقصر وأسهل، مثل:

```
C:\Users\utilisateur\Documents\falak-web
```

مجلّد المخرجات مؤقّت بطبيعته، فالأفضل أن يعيش مشروعك خارجه.
بقيّة الدليل يفترض أنك نقلته إلى `Documents`.

**لا ترفع مجلّد `preview`** — هو نسخة معاينة ثقيلة (٥ ميغابايت) لا لزوم لها على الخادم.
ملف `.gitignore` يستبعده تلقائيًّا، فلا تقلق.

---

## ما تحتاجه قبل البدء

| الأداة | كيف تتأكّد | إن لم تكن موجودة |
|---|---|---|
| Git | `git --version` | نزّله من git-scm.com |
| Node.js | `node --version` | نزّله من nodejs.org |
| حساب GitHub | — | github.com |
| حساب Vercel | — | vercel.com (سجّل بحساب GitHub) |

افتح **PowerShell** (أو الطرفية داخل Cursor) واكتب:

```powershell
cd C:\Users\utilisateur\Documents\falak-web
git --version
node --version
```

إن ظهر رقما نسخة، فأنت جاهز.

---

## الخطوة ١ — جهّز المستودع محلّيًا

```powershell
cd C:\Users\utilisateur\Documents\falak-web
git init -b main
git add .
git commit -m "الفَلَك — أدوات فلكية عربية"
```

للتأكّد أن `preview` استُبعد فعلًا:

```powershell
git ls-files | Select-String "preview"
```

يجب ألّا يظهر شيء. إن ظهر، نفّذ:

```powershell
git rm -r --cached preview
git commit -m "استبعاد مجلّد المعاينة"
```

---

## الخطوة ٢ — ارفعه إلى GitHub

### الطريقة الأولى: بأداة GitHub الرسمية (الأسرع)

إن كان `gh` مثبّتًا لديك:

```powershell
gh auth login
gh repo create falak-web --public --source=. --remote=origin --push
```

انتهى. انتقل إلى الخطوة ٣.

### الطريقة الثانية: يدويًّا

1. افتح **github.com/new**
2. الاسم: `falak-web`
3. اختر **Public**
4. **لا تضع علامة** على `Add a README` ولا `.gitignore` ولا `license` — اتركها كلها فارغة
5. اضغط `Create repository`

ثم في الطرفية (بدّل `<اسم-حسابك>` باسمك الحقيقي):

```powershell
git remote add origin https://github.com/<اسم-حسابك>/falak-web.git
git push -u origin main
```

سيطلب منك تسجيل الدخول. إن طلب كلمة مرور فهو يريد **Personal Access Token** لا كلمة
مرور حسابك: اذهب إلى `github.com/settings/tokens` ← `Generate new token (classic)`
← اختر صلاحية **repo** ← انسخ الرمز والصقه مكان كلمة المرور.

---

## الخطوة ٣ — انشره على Vercel

### الطريقة الأولى: من الموقع (موصى بها)

1. افتح **vercel.com/new**
2. سجّل الدخول بحساب GitHub
3. ستظهر قائمة مستودعاتك — اختر `falak-web` واضغط `Import`
4. **لا تغيّر أي إعداد**. اترك:
   - Framework Preset: `Other`
   - Root Directory: `./`
   - Build Command: فارغ
   - Output Directory: فارغ
5. اضغط `Deploy`
6. انتظر دقيقة إلى دقيقتين

سيعطيك رابطًا مثل `https://falak-web-xxxx.vercel.app`

### الطريقة الثانية: من الطرفية

```powershell
npm i -g vercel
cd C:\Users\utilisateur\Documents\falak-web
vercel login
vercel --prod
```

سيسألك أسئلة — اقبل الإجابات الافتراضية بالضغط على Enter، إلا:
- `Set up and deploy?` → **Y**
- `In which scope?` → حسابك
- `Link to existing project?` → **N**
- `What's your project's name?` → `falak-web`
- `In which directory is your code located?` → **`./`**
- `Want to modify these settings?` → **N**

---

## الخطوة ٤ — تحقّق من النتيجة

افتح هذه الروابط بالترتيب، وسجّل ما رأيت.
بدّل `<موقعك>` بالرابط الذي أعطاك إيّاه Vercel.

### أ — فحص الخادم أوّلًا

```
https://<موقعك>/api/health
```

**المتوقّع:** نصّ بهذا الشكل:

```json
{"ok": true, "cities": 294, "systems": {"whole": "البيوت الكاملة", ...}, "chiron": false}
```

إن ظهر هذا فالمحرّك يعمل، وكل ما بعده سيعمل.
إن ظهر خطأ، انسخه كما هو وأرسله لي — ولا تكمل بقيّة الفحوص.

### ب — الصفحات الخمس

| الرابط | ما يجب أن تراه |
|---|---|
| `https://<موقعك>/` | جدول «السماء الآن» فيه ١٣ صفًّا، ورقاقة ذهبية بطور القمر |
| `https://<موقعك>/bulletin.html` | نشرة اليوم بتوقيت دمشق، مقسّمة إلى أربعة أقسام |
| `https://<موقعك>/chart.html` | نموذج فارغ. أدخل: **١٩٩٠-٠٥-١٧** و **٠٨:٣٠** و **حلب** ثم «احسب الخريطة» |
| `https://<موقعك>/ephemeris.html` | جدول مواقع الكواكب الآن |
| `https://<موقعك>/learn.html` | معجم فيه **٣٥ مصطلحًا** |

### ج — فحوص دقيقة لخريطة حلب

بعد أن تحسب خريطة (١٩٩٠-٠٥-١٧، ٠٨:٣٠، حلب) بنظام **البيوت الكاملة**،
قارن ما تراه بهذا. كل سطر يجب أن يطابق تمامًا:

| البند | القيمة الصحيحة |
|---|---|
| الطالع | `11° 22′ السرطان` |
| وسط السماء | `23° 47′ الحوت` |
| الشمس | `26° 04′ الثور` |
| القمر | `الدلو` |
| زحل | `الجدي` — والكرامة **البيت، الحدّ** |
| المشتري | `السرطان` — والكرامة **الشرف** |
| الأَلْمُطَن | **الزهرة** بمجموع **٢٠** |
| سهم السعادة | `4° 14′ الحمل` |
| عدد الزوايا | **٢٩** (مع الزوايا الصغرى) |
| النجوم الثابتة | أوّلها **الشمس مع رأس الغول** بوجاج ٠.٠٣° |
| عدد البطاقات | **١٤** بطاقة |

### د — فحوص وظيفية

اختبر هذه واحدًا واحدًا:

1. **العجلة الدائرية** — هل ظهرت؟ فيها ثلاث حلقات: البروج بالخارج، ثم أرقام
   من ١ إلى ٢٨ (المنازل القمرية)، ثم البيوت. وخطوط ملوّنة في الوسط.

2. **علامات «؟» الذهبية** — اضغط أيّها. يجب أن تفتح صندوقًا فيه شرح المصطلح.

3. **تبديل نظام البيوت** — غيّر من «البيوت الكاملة» إلى «القبّاني».
   يجب أن يتغيّر البيت العاشر من `0° 00′` إلى `23° 47′ الحوت`.

4. **الأطلس العالمي** — في خانة المدينة اكتب **معرة النعمان** (قرية ليست في
   قائمتي المحلّية). يجب أن تظهر في القائمة المنسدلة. **هذا هو الفحص الأهمّ عندي**
   لأنني لم أستطع اختباره — شبكتي محجوبة عن الخدمة الخارجية.

5. **تحذير التوقيت** — أدخل تاريخ **٢٠٢٤-٠٣-٣١** وساعة **٠٢:٣٠** ومدينة **باريس**.
   يجب أن يظهر تنبيه أحمر: «هذه الساعة لم توجد في ذلك اليوم…»

6. **زر النسخ** — في صفحة النشرة، اضغط «نسخ النصّ». يجب أن يتحوّل إلى «✓ نُسخ».
   الصقه في مكان ما وتأكّد أن النصّ كامل.

7. **الهاتف** — افتح الموقع على هاتفك. الجداول والعجلة يجب أن تُقرأ بلا أفقي.

---

## ما أحتاجه منك عند العودة

انسخ هذا واملأه:

```
رابط الموقع: ..........................................

١. /api/health          → [يعمل / خطأ: ..................]
٢. الصفحات الخمس        → [كلها تعمل / التي فشلت: ........]
٣. أرقام خريطة حلب      → [كلها مطابقة / المختلف: ........]
٤. العجلة الدائرية      → [ظهرت / لم تظهر]
٥. علامات «؟»           → [تعمل / لا تعمل]
٦. تبديل نظام البيوت    → [يعمل / لا يعمل]
٧. «معرة النعمان»       → [ظهرت / لم تظهر]        ← الأهمّ
٨. تحذير ٢٠٢٤-٠٣-٣١     → [ظهر / لم يظهر]
٩. زر النسخ             → [يعمل / لا يعمل]
١٠. الهاتف              → [مقروء / مشاكل: .........]

ملاحظات أخرى: ..........................................
```

إن ظهر أي خطأ، خذ لقطة شاشة أو انسخ نصّ الخطأ حرفيًّا.
وللأخطاء الخفيّة: افتح سجلّ Vercel من
`vercel.com` ← مشروعك ← `Logs`، وانسخ آخر عشرة أسطر.

---

## إن تعثّرت — حلول المشكلات الشائعة

### البناء فشل و`pyswisseph` هو السبب

نادر، لكن إن حدث فأضف ملفًا اسمه `runtime.txt` في جذر المشروع فيه سطر واحد:

```
python-3.12
```

ثم:

```powershell
git add runtime.txt
git commit -m "تثبيت نسخة بايثون"
git push
```

### الصفحات تظهر لكن `/api/health` يعطي 404

معناه أن Vercel لم يقرأ `vercel.json`. تأكّد أنه في **جذر** المشروع لا داخل مجلّد،
وأن `Root Directory` في إعدادات Vercel هو `./`.

### الصفحات لا تظهر و`/api/health` يعمل

الاحتياط الذي بنيته يمنع هذا: الدالة نفسها تخدم الصفحات.
إن حدث رغم ذلك، جرّب `https://<موقعك>/api/../index.html` وأخبرني بالنتيجة.

### `git push` يرفض ويقول `Authentication failed`

استعمل Personal Access Token بدل كلمة المرور — الشرح في الخطوة ٢.

### الخطّ العربي يظهر بشكل رديء

الخطوط تُحمَّل من Google Fonts. إن كانت محجوبة عندك فسيتراجع الموقع إلى خطّ
النظام تلقائيًّا — الموقع يعمل، لكن المظهر أقلّ أناقة.

### الخريطة بطيئة (أكثر من ٥ ثوانٍ)

هذا طبيعي في أوّل طلب فقط («البدء البارد»). الطلبات التالية تكون أسرع بكثير،
والنتائج المتطابقة تُخزَّن في ذاكرة Vercel ساعةً كاملة.

---

## بعد أن ينجح النشر

- **نطاق خاص:** من إعدادات المشروع في Vercel ← `Domains`، يمكنك ربط نطاقك.
- **تحديث الموقع لاحقًا:** أي `git push` إلى `main` ينشر تلقائيًّا خلال دقيقة.
- **خيرون:** إن أردت إظهاره، أنشئ مجلّد `ephe` في جذر المشروع وضع فيه ملف
  `seas_18.se1` من موقع Astrodienst، ثم ارفعه. سيظهر تلقائيًّا في الجداول.
